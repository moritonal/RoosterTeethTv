name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: NPM Install
      run: npm install
      
    - name: Run a one-line script
      run: npm run deploy
